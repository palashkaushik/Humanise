services:
  - type: web
    name: humanise
    runtime: python
    buildCommand: pip install uv && uv pip install --system .[all]
    startCommand: uvicorn humanise.web:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
